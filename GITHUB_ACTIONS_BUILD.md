# Сборка APK через GitHub Actions

## Быстрый старт

У вас есть 2 способа собрать APK с поддержкой прокси через GitHub Actions:

### Способ 1: Использовать существующую инфраструктуру Kiwi (БЫСТРО)

Если у вас есть доступ к buildbot серверу Kiwi:

1. Перейдите в **Actions** → **Simple: Build with Proxy (using Kiwi buildbot)**
2. Нажмите **Run workflow**
3. Подождите ~30-60 минут
4. Скачайте APK по ссылке: `https://build.find.kiwi/apks/[RUN_ID]/out.apk`

**Требования:**
- Секрет `BUILD_KEY` должен быть настроен в репозитории

### Способ 2: Полная сборка на GitHub (МЕДЛЕННО, но работает всегда)

Если у вас нет доступа к buildbot:

1. Перейдите в **Actions** → **Build APK with Proxy Support**
2. Нажмите **Run workflow**
3. Выберите параметры:
   - **Architecture**: `arm64` (для большинства современных устройств)
   - **Build type**: `release` (для финальной версии) или `debug` (для тестирования)
4. Нажмите **Run workflow**
5. Подождите ~2-4 часа (первая сборка может занять больше времени)
6. Скачайте APK из **Artifacts** или **Releases**

## Подробная инструкция

### Шаг 1: Форкнуть репозиторий

```bash
# Форкните репозиторий через GitHub UI или:
gh repo fork kiwibrowser/src.next --clone
cd src.next
```

### Шаг 2: Добавить файлы прокси

Все файлы уже добавлены в репозиторий:
- ✅ `chrome/browser/proxy/*.cc` и `*.h` - C++ код
- ✅ `chrome/android/java/src/org/chromium/chrome/browser/proxy/*.java` - Java код
- ✅ `chrome/android/java/res/layout/activity_proxy_settings.xml` - UI layout
- ✅ `.github/workflows/build_proxy_apk.yml` - GitHub Actions workflow

### Шаг 3: Запустить сборку

#### Через GitHub UI:

1. Откройте ваш форк на GitHub
2. Перейдите во вкладку **Actions**
3. Выберите workflow **"Build APK with Proxy Support"**
4. Нажмите **"Run workflow"** справа
5. Выберите параметры:
   - **architecture**: 
     - `arm64` - для современных устройств (рекомендуется)
     - `arm` - для старых устройств
     - `x86` / `x64` - для эмуляторов
   - **build_type**:
     - `release` - оптимизированная версия (меньше размер, быстрее работает)
     - `debug` - версия для отладки (больше размер, есть отладочная информация)
6. Нажмите зеленую кнопку **"Run workflow"**

#### Через GitHub CLI:

```bash
# Установить GitHub CLI (если еще не установлен)
# Ubuntu/Debian:
sudo apt install gh

# Авторизоваться
gh auth login

# Запустить workflow
gh workflow run "Build APK with Proxy Support" \
  -f architecture=arm64 \
  -f build_type=release

# Посмотреть статус
gh run list --workflow="Build APK with Proxy Support"

# Посмотреть логи
gh run view --log
```

### Шаг 4: Скачать APK

#### Способ 1: Из Artifacts (для любой сборки)

1. Откройте завершенный workflow run
2. Прокрутите вниз до секции **Artifacts**
3. Скачайте `kiwi-proxy-[architecture]-[build_type].zip`
4. Распакуйте ZIP и найдите APK

#### Способ 2: Из Releases (только для release сборок)

1. Перейдите во вкладку **Releases**
2. Найдите релиз `proxy-build-[RUN_ID]`
3. Скачайте `kiwi-browser-proxy-[architecture]-[RUN_ID].apk`

#### Способ 3: Через GitHub CLI

```bash
# Скачать последний artifact
gh run download

# Или скачать конкретный run
gh run download [RUN_ID]
```

### Шаг 5: Установить APK на устройство

#### Через ADB:

```bash
# Подключить устройство по USB
adb devices

# Установить APK
adb install -r kiwi-browser-proxy-arm64-*.apk
```

#### Через файловый менеджер:

1. Скопируйте APK на устройство
2. Откройте файловый менеджер
3. Нажмите на APK файл
4. Разрешите установку из неизвестных источников (если требуется)
5. Нажмите "Установить"

## Настройка прокси после установки

1. Откройте браузер
2. Нажмите меню (три точки)
3. Выберите **Settings** (Настройки)
4. Найдите **Proxy Settings** (Настройки прокси)
5. Включите прокси
6. Настройте:
   - **Type**: HTTP / HTTPS / SOCKS4 / SOCKS5
   - **Host**: адрес прокси сервера
   - **Port**: порт прокси
   - **Username** / **Password**: (опционально)
7. Нажмите **Test** для проверки
8. Нажмите **Save** для сохранения

## Проверка работы прокси

Откройте в браузере:
- https://whatismyipaddress.com/
- https://whoer.net/
- https://2ip.ru/

Ваш IP должен измениться на IP прокси сервера.

## Архитектуры устройств

| Архитектура | Устройства | Рекомендация |
|-------------|-----------|--------------|
| **arm64** | Большинство современных Android устройств (2015+) | ✅ Рекомендуется |
| **arm** | Старые Android устройства, устройства с малым объемом RAM | Для совместимости |
| **x86** | Эмуляторы, некоторые планшеты Intel | Для тестирования |
| **x64** | Эмуляторы x86_64 | Для тестирования |

Как узнать архитектуру вашего устройства:
```bash
adb shell getprop ro.product.cpu.abi
```

## Оптимизация времени сборки

### Использовать кэш (для повторных сборок)

GitHub Actions автоматически кэширует зависимости, но первая сборка всегда долгая.

### Собрать только одну архитектуру

Вместо сборки всех архитектур (arm, arm64, x86, x64), соберите только нужную:
- Для большинства устройств: `arm64`
- Для старых устройств: `arm`

### Использовать debug сборку для тестирования

Debug сборки собираются быстрее (~30-40% быстрее), но APK больше по размеру.

## Устранение проблем

### Ошибка: "No space left on device"

GitHub Actions runners имеют ограниченное место на диске. Workflow уже включает очистку, но если проблема сохраняется:

1. Используйте только одну архитектуру
2. Используйте debug сборку (она меньше)
3. Попробуйте запустить workflow снова

### Ошибка: "Build failed"

Проверьте логи:
1. Откройте failed workflow run
2. Нажмите на failed step
3. Прочитайте error message

Частые причины:
- Отсутствуют зависимости (workflow должен их установить автоматически)
- Ошибки компиляции в коде прокси
- Недостаточно места на диске

### APK не устанавливается

1. Проверьте, что архитектура соответствует вашему устройству
2. Удалите старую версию Kiwi Browser
3. Разрешите установку из неизвестных источников
4. Проверьте, что APK не поврежден (скачайте заново)

### Прокси не работает

1. Проверьте настройки прокси (хост, порт)
2. Убедитесь, что прокси сервер работает
3. Проверьте логи: `adb logcat | grep -i proxy`
4. Попробуйте другой тип прокси (HTTP вместо SOCKS)

## Время сборки

| Тип сборки | Первая сборка | Повторная сборка |
|------------|---------------|------------------|
| Debug (одна архитектура) | ~2-3 часа | ~30-60 минут |
| Release (одна архитектура) | ~3-4 часа | ~1-2 часа |
| Все архитектуры | ~8-12 часов | ~4-6 часов |

**Примечание:** Время может варьироваться в зависимости от загрузки GitHub Actions runners.

## Лимиты GitHub Actions

- **Free tier**: 2000 минут/месяц для приватных репозиториев
- **Public repositories**: Неограниченно
- **Concurrent jobs**: До 20 одновременно (Free tier)
- **Job timeout**: 6 часов максимум

Для сборки Chromium рекомендуется использовать **публичный репозиторий**.

## Альтернативы

Если GitHub Actions не подходит:

1. **Локальная сборка** - следуйте `BUILD_APK_GUIDE.md`
2. **Google Cloud Build** - более мощные машины
3. **CircleCI** - альтернативный CI/CD сервис
4. **Travis CI** - еще один вариант

## Автоматизация

### Автоматическая сборка при push

Добавьте в `.github/workflows/build_proxy_apk.yml`:

```yaml
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:
```

### Автоматическая сборка по расписанию

```yaml
on:
  schedule:
    - cron: '0 0 * * 0'  # Каждое воскресенье в полночь
  workflow_dispatch:
```

### Сборка для всех архитектур

Создайте matrix build:

```yaml
strategy:
  matrix:
    architecture: [arm, arm64, x86, x64]
```

## Полезные команды

```bash
# Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/src.next.git

# Запустить workflow
gh workflow run "Build APK with Proxy Support" -f architecture=arm64 -f build_type=release

# Посмотреть статус
gh run list

# Скачать artifacts
gh run download

# Посмотреть логи
gh run view --log

# Отменить запущенный workflow
gh run cancel [RUN_ID]
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи workflow
2. Прочитайте `BUILD_APK_GUIDE.md`
3. Создайте issue в репозитории
4. Проверьте существующие issues

---

**Готово!** Теперь вы можете собирать APK с поддержкой прокси через GitHub Actions! 🎉
