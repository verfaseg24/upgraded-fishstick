# 🌐 Автоматические бесплатные прокси

## Обзор

Система автоматически получает, проверяет и предоставляет бесплатные прокси из API GetFreeProxy.

### Возможности

- ✅ **Автоматическое получение** - прокси загружаются из API
- ✅ **4 страны** - Германия 🇩🇪, США 🇺🇸, Швеция 🇸🇪, Финляндия 🇫🇮
- ✅ **3 страницы на страну** - до 30+ прокси на страну
- ✅ **Автоматическая проверка** - все прокси тестируются перед показом
- ✅ **Кэширование** - обновление раз в 24 часа
- ✅ **Фильтрация** - только качественные прокси (uptime > 80%, response < 1s)
- ✅ **Один клик** - подключение к прокси одним нажатием

## Как это работает

### 1. Получение прокси

```
API Request → GetFreeProxy API
├── Германия (DE) - страницы 1, 2, 3
├── США (US) - страницы 1, 2, 3
├── Швеция (SE) - страницы 1, 2, 3
└── Финляндия (FI) - страницы 1, 2, 3
```

### 2. Фильтрация

Отбираются только качественные прокси:
- **Uptime** > 80%
- **Response time** < 1 секунда
- **Anonymity** - любой уровень

### 3. Проверка

Каждый прокси тестируется:
```java
HTTP HEAD request → google.com
├── Timeout: 5 секунд
├── Success (200) → ✅ Добавить в список
└── Fail → ❌ Пропустить
```

### 4. Кэширование

- Прокси сохраняются в SharedPreferences
- Обновление раз в 24 часа
- Можно принудительно обновить кнопкой "Обновить"

## Использование

### Для пользователей

1. Откройте браузер
2. Меню → Settings → Proxy Settings
3. Нажмите **"🌐 Бесплатные прокси (авто)"**
4. Подождите загрузки списка
5. Выберите прокси из списка
6. Прокси автоматически активируется

### Информация о прокси

Каждый прокси показывает:
```
IP:PORT (Страна)
↑ Uptime% | ⚡ Response ms | PROTOCOL
```

Пример:
```
185.123.45.67:8080 (🇩🇪 Германия)
↑ 95% | ⚡ 120ms | HTTP
```

### Обновление списка

- **Автоматически**: раз в 24 часа
- **Вручную**: кнопка "Обновить"

## Архитектура

### Java классы

#### ProxyFetcher.java
Основной класс для получения прокси:

```java
// Получить прокси (с кэшированием)
ProxyFetcher.fetchProxies(new FetchCallback() {
    @Override
    public void onSuccess(List<ProxyInfo> proxies) {
        // Обработка списка прокси
    }
    
    @Override
    public void onError(String error) {
        // Обработка ошибки
    }
});

// Принудительное обновление
ProxyFetcher.forceUpdate(callback);
```

#### AutoProxyActivity.java
UI для выбора прокси:
- Показывает список прокси
- Позволяет выбрать прокси
- Автоматически применяет конфигурацию

### API Integration

#### Endpoint
```
GET https://api.getfreeproxy.com/v1/proxies
```

#### Headers
```
Authorization: Bearer 019c5236e4417ddb8e9247c61c33336a
Accept: application/json
```

#### Parameters
```
?country=DE&page=1
```

#### Response
```json
[
  {
    "ip": "185.123.45.67",
    "port": 8080,
    "protocol": "http",
    "countryCode": "DE",
    "uptime": 95.5,
    "responseTime": 0.12,
    "anonymity": "elite"
  }
]
```

## Конфигурация

### Изменить страны

В `ProxyFetcher.java`:
```java
private static final String[] COUNTRIES = {"DE", "US", "SE", "FI"};
```

Доступные коды стран:
- DE - Германия
- US - США
- SE - Швеция
- FI - Финляндия
- UK - Великобритания
- FR - Франция
- NL - Нидерланды
- и другие...

### Изменить количество страниц

```java
private static final int PAGES_PER_COUNTRY = 3;
```

### Изменить интервал обновления

```java
private static final long UPDATE_INTERVAL = 24 * 60 * 60 * 1000; // 24 часа
```

Примеры:
- 12 часов: `12 * 60 * 60 * 1000`
- 6 часов: `6 * 60 * 60 * 1000`
- 1 час: `60 * 60 * 1000`

### Изменить критерии фильтрации

```java
private static List<ProxyInfo> filterQualityProxies(List<ProxyInfo> proxies) {
    List<ProxyInfo> filtered = new ArrayList<>();
    
    for (ProxyInfo proxy : proxies) {
        // Изменить условия здесь
        if (proxy.uptime > 80 && proxy.responseTime < 1.0) {
            filtered.add(proxy);
        }
    }
    
    return filtered;
}
```

## Интеграция

### Добавить в BUILD.gn

В `chrome/android/BUILD.gn`:
```gn
java_files += [
  "java/src/org/chromium/chrome/browser/proxy/ProxyFetcher.java",
  "java/src/org/chromium/chrome/browser/proxy/AutoProxyActivity.java",
]
```

### Добавить в AndroidManifest.xml

```xml
<activity
    android:name="org.chromium.chrome.browser.proxy.AutoProxyActivity"
    android:label="Auto Proxy"
    android:exported="false" />
```

### Добавить разрешения

В `AndroidManifest.xml`:
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

## Безопасность

### API Key

API ключ хранится в коде:
```java
private static final String API_KEY = "019c5236e4417ddb8e9247c61c33336a";
```

**Для production:**
- Переместите ключ в BuildConfig
- Используйте ProGuard для обфускации
- Или храните на сервере и получайте динамически

### Проверка прокси

Все прокси проверяются перед показом:
```java
private static boolean testProxy(ProxyInfo proxy) {
    // Тестовое подключение к google.com
    // Timeout: 5 секунд
    // Только HEAD request (без загрузки контента)
}
```

### Кэширование

Прокси хранятся в SharedPreferences:
```java
SharedPreferences prefs = context.getSharedPreferences("proxy_fetcher", MODE_PRIVATE);
```

**Для production:**
- Используйте EncryptedSharedPreferences
- Добавьте TTL для каждого прокси
- Периодически перепроверяйте кэшированные прокси

## Производительность

### Оптимизации

1. **Кэширование** - запросы к API раз в 24 часа
2. **Фоновая загрузка** - не блокирует UI
3. **Ограничение проверки** - проверяются только первые 20 прокси
4. **Задержка между запросами** - 500ms между страницами

### Время загрузки

- **Первая загрузка**: 10-30 секунд
- **Из кэша**: мгновенно
- **Принудительное обновление**: 10-30 секунд

### Трафик

- **Один запрос к API**: ~1-5 KB
- **Всего запросов**: 12 (4 страны × 3 страницы)
- **Общий трафик**: ~12-60 KB

## Устранение проблем

### Прокси не загружаются

**Причины:**
1. Нет интернета
2. API недоступен
3. Неверный API ключ
4. Превышен лимит запросов

**Решение:**
- Проверьте подключение к интернету
- Проверьте логи: `adb logcat | grep ProxyFetcher`
- Попробуйте обновить список вручную

### Все прокси не работают

**Причины:**
1. Прокси устарели
2. Прокси заблокированы провайдером
3. Строгие критерии фильтрации

**Решение:**
- Принудительно обновите список
- Измените критерии фильтрации
- Добавьте больше стран

### Медленная загрузка

**Причины:**
1. Медленное интернет-соединение
2. Много прокси для проверки
3. Медленный API

**Решение:**
- Уменьшите количество страниц
- Уменьшите количество проверяемых прокси
- Увеличьте timeout проверки

## Расширенное использование

### Автоматическое подключение

Подключаться к лучшему прокси при запуске:

```java
ProxyFetcher.fetchProxies(new FetchCallback() {
    @Override
    public void onSuccess(List<ProxyInfo> proxies) {
        if (!proxies.isEmpty()) {
            // Выбрать лучший прокси
            ProxyInfo best = proxies.get(0);
            
            // Применить
            ProxyConfig config = new ProxyConfig();
            config.setEnabled(true);
            config.setHost(best.ip);
            config.setPort(best.port);
            ProxyManager.getInstance().setProxyConfig(config);
        }
    }
});
```

### Ротация прокси

Автоматически менять прокси каждые N минут:

```java
Handler handler = new Handler();
Runnable rotateProxy = new Runnable() {
    int currentIndex = 0;
    
    @Override
    public void run() {
        List<ProxyInfo> proxies = getCachedProxies();
        if (!proxies.isEmpty()) {
            ProxyInfo proxy = proxies.get(currentIndex % proxies.size());
            applyProxy(proxy);
            currentIndex++;
        }
        handler.postDelayed(this, 5 * 60 * 1000); // 5 минут
    }
};
handler.post(rotateProxy);
```

### Статистика использования

Отслеживать, какие прокси работают лучше:

```java
public class ProxyStats {
    private Map<String, Integer> successCount = new HashMap<>();
    private Map<String, Integer> failCount = new HashMap<>();
    
    public void recordSuccess(String proxyId) {
        successCount.put(proxyId, successCount.getOrDefault(proxyId, 0) + 1);
    }
    
    public void recordFail(String proxyId) {
        failCount.put(proxyId, failCount.getOrDefault(proxyId, 0) + 1);
    }
    
    public double getSuccessRate(String proxyId) {
        int success = successCount.getOrDefault(proxyId, 0);
        int fail = failCount.getOrDefault(proxyId, 0);
        return (double) success / (success + fail);
    }
}
```

## API Limits

GetFreeProxy API лимиты:
- **Бесплатный план**: 100 запросов/день
- **Наша конфигурация**: 12 запросов/обновление
- **Обновлений в день**: ~8 (при обновлении раз в 24 часа)

**Рекомендации:**
- Не обновляйте чаще раза в час
- Используйте кэширование
- Обрабатывайте ошибку 429 (Rate Limit)

## Заключение

Система автоматических прокси предоставляет:
- ✅ Простой способ получить рабочие прокси
- ✅ Автоматическую проверку и фильтрацию
- ✅ Удобный UI для выбора
- ✅ Кэширование для экономии трафика
- ✅ Обновление раз в 24 часа

**Готово к использованию!** 🚀
